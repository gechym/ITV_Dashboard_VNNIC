type TMenuItem = {
  title: string;
  path: string;
  icon?: React.ReactNode;
};

type TOptionItem = {
  value: string;
  label: React.ReactNode;
};

type TDomainType = {
  id: string;
  domain: string;
  label: string;
  type: string;
  reason: string;
  date: string;
  status: string;
};
